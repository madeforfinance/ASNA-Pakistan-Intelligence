name: ASNA Daily Intelligence

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: asna-intelligence
  cancel-in-progress: false

jobs:
  daily:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run daily digest
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          DISCORD_GUILD_ID: ${{ secrets.DISCORD_GUILD_ID }}
          INTELLIGENCE_DB: data/intelligence.db
        run: python news/daily_digest.py

      - name: Run source health
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          DISCORD_GUILD_ID: ${{ secrets.DISCORD_GUILD_ID }}
          INTELLIGENCE_DB: data/intelligence.db
        run: python news/source_health.py

      - name: Run system health
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          DISCORD_GUILD_ID: ${{ secrets.DISCORD_GUILD_ID }}
          INTELLIGENCE_DB: data/intelligence.db
        run: python news/system_health.py

      - name: Persist SQLite database
        run: |
          git config user.name 'ASNA Pakistan Intelligence Bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/intelligence.db
          if git diff --cached --quiet; then
            echo 'No database changes to commit.'
          else
            git commit -m 'chore: update daily intelligence state'
            git push
          fi
